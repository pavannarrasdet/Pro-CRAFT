package onboarding.runners;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.interactions.Actions;

import procraft.core.utilities.ui.SeleniumHelper;

public class UHCTest {

	public static void main(String[] args) {

		/*
		 * // TODO Auto-generated method stub //SeleniumHelper.launchDriver("ie");
		 * SeleniumHelper.launchChromeDriver();
		 * SeleniumHelper.navigateToURL("https://new-wiki.optum.com/login.action");
		 * System.out.println(SeleniumHelper.getTitle()); WebDriver driver =
		 * SeleniumHelper.getDriver();
		 * driver.findElement(By.id("os_username")).sendKeys("pnarra");
		 * driver.findElement(By.id("os_password")).sendKeys("N@rr@138%");
		 * driver.findElement(By.name("login")).click();
		 * System.out.println(SeleniumHelper.getTitle());
		 * driver.get("https://new-wiki.optum.com/display/CirrusNew/Demo+Template");
		 * System.out.println(SeleniumHelper.getTitle());
		 * driver.findElement(By.id("editPageLink")).click(); WebElement tb =
		 * driver.findElements(By.
		 * xpath("//p[contains(.,'Gateway Performance')]/following::table")).get(0); int
		 * count = tb.findElements(By.tagName("th")).size(); System.out.println(count);
		 * tb.findElement(By.xpath("//th[text()='Activities']")).click();
		 * driver.findElement(By.
		 * xpath("//button[contains(@data-tooltip,'Insert row below')]")).click();
		 * driver.findElement(By.name("confirm")).click();
		 */
		ChromeDriver drivers = new ChromeDriver();
		

	}

}
